# Контракты данных (версионированные)

- Каталог `v1/` - текущая версия Pydantic-моделей для сообщений брокера и API.
- При breaking-change добавляем `v2/` и адаптеры в воркерах - не ломаем v1 consumers в одном коммите.
