# Процесс MCP-сервера: безопасные операции Playwright (goto, wait, extract, screenshot).
# Агенты не получают сырой JS без allowlist - только вызовы из этого слоя.
# Связь с очередью extraction - через отдельные команды или публикацию задач, не дублируя hunter-воркер без нужды.
